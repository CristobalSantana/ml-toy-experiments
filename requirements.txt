numpy>=1.24
scipy>=1.10
matplotlib>=3.7
torch>=2.2   # CPU is enough and deliberately used for training determinism
pandas>=2.0  # comparison-table aggregation/export only
pyyaml>=6.0  # experiments/*/config.yaml parsing
openpyxl>=3.1  # datasets/ch/central_bank_hpi: read the BDE IPV Excel export
scikit-learn>=1.4  # experiments/drift-detector-overlap: the classifier two-sample test
joblib>=1.3  # same: trials are independent and farmed out across cores
pyarrow>=15.0  # datasets/us/nyc_taxi: the TLC publishes monthly parquet
